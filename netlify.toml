[build]
  command = "npm install && npm run db:migrate && npm run db:seed && npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
  NEXT_PUBLIC_SITE_URL = "https://aicodegamecursor.netlify.app"
